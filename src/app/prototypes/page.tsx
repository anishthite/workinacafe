import { CafePrototypeLab } from "@/components/cafe-prototype-lab";

export const metadata = {
  title: "workina.cafe — UI directions",
  description: "Five alternate interface directions for finding a café to work from.",
};

export default function PrototypesPage() {
  return <CafePrototypeLab />;
}
